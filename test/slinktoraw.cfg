process SLINKTORAW = 
{

   untracked PSet maxEvents = {untracked int32 input = -1 }

   # Input source
   include "IORawData/SiPixelInputSources/data/PixelSLinkDataInputSource.cfi"
   replace PixelSLinkDataInputSource.fileNames = {"file:/afs/cern.ch/user/d/dkotlins/public/data/PhysicsData_511.dmp"}
   replace PixelSLinkDataInputSource.fedid = 0

   # Message Logger
   service = MessageLogger {	
	untracked vstring destinations = {"cout"}
	untracked PSet cout = {untracked string threshold = "DEBUG"}
   }


   #module rawdump = PixelRawDump{
   #} 

   # module execution
   #path p = { rawdump }
   
   # Output module
    module out = PoolOutputModule
   { 		
   	untracked string fileName = "/afs/cern.ch/user/d/dkotlins/public/data/PhysicsData_511_0.root"
   }

   # Timing service
   # service = Timing {}

   endpath e = { out }
}
